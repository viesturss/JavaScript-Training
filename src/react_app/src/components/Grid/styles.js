const styles = theme => ({
  container: {
    background: "#efefef",
    overflow: "auto"
  },
  grid: {
    width: 800
  },
  row: {
    width: "100%",
    display: "table"
  }
});

export default styles;
